

# cap = open(*pcap)  with module for capfiles

class Packet:
    pass

# pac_var = Packet.parse(cap)

class Activity(Packet, object):
    def __init__(self):
        self.src_addr = obj.Packet

# act_var = Activity(pac_var).resolve
>>> {'ip1':3000 pacs,'ipN+1':2400 pacs}
>>>
